// Demo component to verify Reviewbot's full before/after + AI review path — safe to delete.
export function E2EProbe({ title }: { title: string }) {
  return <h2 className="text-lg font-semibold">{title}</h2>;
}
